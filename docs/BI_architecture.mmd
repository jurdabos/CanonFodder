%% c9r v0.6 – BI Reference Architecture (post‑migration)
flowchart TB

%% ───── Source layer ─────
subgraph Source_Systems["Source systems"]
    direction LR
    lfAPI["HTTP/lfAPI  ⤵️\n• Last.fm scrobble JSON\n• Artist, Album, Song, uts, artist_mbid"]
    lblink["HTTP/lblink  ⤵️\n• ListenBrainz listen JSON\n• artist, track, release, listened_at, artist_mbid"]
    mbAPI["HTTP/mbAPI  ⤵️\n• MusicBrainz artist JSON\n• alias, primary_alias, comment, country"]
    userCLI["Click CLI  ⤵️\n• c9r canonise / review\n• user‑country timeline"]
end

%% ───── Integration / Staging layer ─────
subgraph Staging["Integration layer  ➜  Python ETL (Click CLI + helpers)"]
    direction LR
    extract["Extract"] --- transform["Transform\n(normalise_scrobble_df)"] --- load["Load\n(append_to_parquet)"]
end

%% ───── Data‑storage layer ─────
subgraph DWH["Data‑storage layer  ➜  Parquet store (zstd) + DuckDB query layer"]
    direction LR
    pqstore["PQ/ Parquet files\n───────────────\n• scrobble.parquet\n• artist_info.parquet\n• avc.parquet\n• uc.parquet\n• c.parquet"]
    duckdb["helpers/query.py\n───────────────\nDuckDB‑over‑Parquet\n• top_artists\n• scrobble_count\n• unique_artists\n• ascii_freq"]
    pqstore --- duckdb
end

%% ───── ML layer ─────
subgraph MLPipeline["ML pipeline"]
    direction LR
    xgb["XGBoost model\nML/xgb.json\nML/xgb_columns.json"]
    fastapi["FastAPI model server\ncorefunc/model_server.py\n/predict  /health"]
    xgb --- fastapi
end

%% ───── Output / star schema layer ─────
subgraph Output["Output layer  ➜  Star schema (Parquet)"]
    star["Star schema views\n• fact_scrobble (Y/M/D expanded)\n• dim_artist_info\n• dim_avc\n• dim_uc\n• dim_country_code"]
end

%% ───── Front‑end / analytics layer ─────
subgraph Frontend["Frontend"]
    viz["Python dashboards\nPlotly Express / Matplotlib / seaborn"]
end

%% ───── Metadata side‑rail ─────
subgraph Metadata["Metadata management"]
    mapping["Mapping\n Artist → artist_name\n Album  → album_title\n Song   → track_title\n uts    → pd.Timestamp"]
end

%% ───── Orchestration ─────
Prefect["Prefect 3.x\nflows/cf_ingest.py\nweekly_ingest_flow"]:::orch
classDef orch fill:#E8EAF6,stroke:#3F51B5,stroke-width:2px,color:#1A237E

%% ───── Configure layout directions ─────
Source_Systems --> Staging --> DWH --> Output --> Frontend
DWH --> MLPipeline --> Frontend

%% ───── Data flow arrows ─────
lfAPI --> extract
lblink --> extract
mbAPI --> extract
userCLI --> extract

extract --> transform --> load --> pqstore
duckdb --> star --> viz
duckdb --> xgb
fastapi --> viz

%% metadata dotted links
Metadata --- Source_Systems
mapping -. manages .-> extract
mapping -. manages .-> transform
mapping -. governs .-> pqstore
mapping -. documents .-> star

%% orchestration
Prefect -.-> Staging
Prefect -.-> DWH
Prefect -.-> MLPipeline
